name: Fail Test

on:
  workflow_dispatch:

jobs:
  fail-job:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Hello!"
      - name: Fail on purpose
        run: exit 1
