name: Check UR Listings and Ping Render

on:
  schedule:
    - cron: '57 2 * * *'  # JSTで11:57（UTCで2:57）

  workflow_dispatch:     # 手動実行もOK！

jobs:
  check-and-ping:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 リポジトリをチェックアウト
        uses: actions/checkout@v3

      - name: 🐍 Pythonをセットアップ
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 依存パッケージをインストール
        run: pip install -r requirements.txt

      - name: 🔍 新着チェック＆Render起動
        env:
          RENDER_WEBHOOK_URL: ${{ secrets.RENDER_WEBHOOK_URL }}
          LINE_CHANNEL_ACCESS_TOKEN: ${{ secrets.LINE_CHANNEL_ACCESS_TOKEN }}
          LINE_USER_ID: ${{ secrets.LINE_USER_ID }}
          LINE_GROUP_ID: ${{ secrets.LINE_GROUP_ID }}
        run: python check_ur.py

      - name: 🔍 Seleniumで詳細チェック
        env:
          CHANNEL_ACCESS_TOKEN: ${{ secrets.LINE_CHANNEL_ACCESS_TOKEN }}
          USER_ID: ${{ secrets.LINE_USER_ID }}
        run: python ur_checker.py
