name: Check UR Listings and Ping Render

on:
  schedule:
    - cron: '57 2 * * *'  # JSTで11:57（UTCで2:57）に毎日実行
  workflow_dispatch:       # 手動実行も可能

jobs:
  check-and-ping:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 リポジトリをチェックアウト
        uses: actions/checkout@v3

      - name: 🐍 Pythonをセットアップ（バージョン3.10）
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 依存パッケージをインストール
        run: pip install -r requirements.txt

      - name: 🔍 新着チェック＆Render起動（静的HTML解析）
        env:
          RENDER_WEBHOOK_URL: ${{ secrets.RENDER_WEBHOOK_URL }}
          LINE_CHANNEL_ACCESS_TOKEN: ${{ secrets.LINE_CHANNEL_ACCESS_TOKEN }}
          LINE_USER_ID: ${{ secrets.LINE_USER_ID }}
          LINE_GROUP_ID: ${{ secrets.LINE_GROUP_ID }}
        run: python check_ur.py

      - name: 🔍 Seleniumで詳細チェック（動的ページ対応）
        env:
          LINE_CHANNEL_ACCESS_TOKEN: ${{ secrets.LINE_CHANNEL_ACCESS_TOKEN }}
          LINE_GROUP_ID: ${{ secrets.LINE_GROUP_ID }}
        run: python ur_checker.py
